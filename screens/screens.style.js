import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    actionContainer:{
      flexDirection: 'column',
      alignItems: 'center',
      padding:5,
    },
    iconText:{
      fontSize: 10,
    },
    rightContainer:{
      flexDirection: 'row',
    },
  });
