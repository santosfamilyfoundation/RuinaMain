/*
  This file is used by the appNavigation component to render the Map screen.
*/
import React from "react";
import Map from "./Map";

export const MapScreen = ({ navigation }) => {
  return <Map navigation={navigation} />;
};
