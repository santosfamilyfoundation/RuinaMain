import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    centeredContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    styledButton: {
      margin: 10,
    },
    bottomBar:{
      padding: 10,
      justifyContent: 'center',
      alignItems: 'center',
    },
    bottomBarText:{
      color: '#A9A9A9',
    },
  });
