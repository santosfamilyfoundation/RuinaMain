import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    modalContent: {
      padding: 5,
      marginBottom: 10,
      alignItems: 'center'
    },
    helperText: {
      margin: 1,
      marginBottom: 20
    }
});