import { PulseAnimation, ShakeAnimation, ZoomAnimation, } from '../animation';
export function getIconAnimation(animation, config) {
    switch (animation) {
        case 'zoom':
            return new ZoomAnimation(config);
        case 'pulse':
            return new PulseAnimation(config);
        case 'shake':
            return new ShakeAnimation(config);
    }
}
//# sourceMappingURL=iconAnimation.js.map