import React from 'react';
export class EvaIcon {
    constructor(content) {
        this.content = content;
    }
    toReactElement(props) {
        const Icon = this.content;
        // @ts-ignore - Eva maps icon color to `tintColor`
        const { tintColor, ...restProps } = props;
        return (React.createElement(Icon, Object.assign({ fill: tintColor }, restProps)));
    }
}
//# sourceMappingURL=evaIcon.component.js.map